[package]
name = "Lyricly"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {version = "1.48.0", features = ["full"]}
windows = {version = "0.62.2", features = ["Media_Control", "Foundation"]}
